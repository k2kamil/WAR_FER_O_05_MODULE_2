function addTheSameNumbers(number, array) {
    var sum = 0;
    for (let i = 0; i < array.length; i++) {
        if (array[i] == number) {
            sum = sum + array[i];
        }
    }
    return null;
}


teoretycznie jest dobrze